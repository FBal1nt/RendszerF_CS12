@page
@model JegyMester.Pages.Screening.IndexModel

<p>
    <a asp-page="Create" class="btn btn-success">Create New</a>
    <a asp-page="/AdminIndex" class="btn btn-secondary">Vissza az Admin oldalra</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Screening[0].StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Screening[0].EndTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Screening[0].Room)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Screening[0].Film)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Screening) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Film.Director)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id" class="link-edit">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id" class="link-details">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id" class="link-delete">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
